#include "main.h"

/**
 * _islower - check for if a letter is lower case
 * @c : input character to check the if is lower case or not
 * Return: 0 or 1
 */

int _islower(int c)
{
	return (c >= 97 && c <= 122);

}
